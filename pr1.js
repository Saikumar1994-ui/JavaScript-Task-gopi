let arr = ["sai", "saikumar", "ravi", "bb", "a"];

// let sortArr = (arr) => {
//   console.log(arr.sort((a, b) => a.length - b.length));
// };
// sortArr(arr);

let arr1 = arr.sort((a, b) => a.length - b.length);
console.log(arr1);
